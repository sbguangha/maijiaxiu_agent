# 影刀RPA配置详细步骤（手把手教程）

> 本文档介绍如何在影刀RPA软件中配置自动发送微信的流程

---

## 一、准备工作

### 1.1 环境检查清单

| 项目 | 要求 | 检查方式 |
|------|------|----------|
| 影刀RPA | 已安装并登录 | 打开影刀客户端 |
| 微信PC版 | 已登录且窗口可见 | 微信窗口不要最小化 |
| Python服务 | 已启动 | 浏览器访问 `http://127.0.0.1:8000` |
| 分辨率 | 固定不变 | 不要调整显示器缩放比例 |

### 1.2 启动Python服务

```bash
cd /e/1_CODE/review-picture-agent
python app.py
```

确认看到：`Uvicorn running on http://127.0.0.1:8000`

---

## 二、影刀中创建新流程

### 2.1 新建流程

1. 打开影刀RPA客户端
2. 点击【新建流程】
3. 选择【新建空白流程】
4. 输入流程名称：`微信自动发送评价`
5. 点击【确定】

### 2.2 流程架构概览

```
【主流程】微信自动发送评价
  ├─ 1. 初始化变量
  ├─ 2. 循环：拉取任务
  │    ├─ 2.1 HTTP请求：获取任务
  │    ├─ 2.2 判断：是否有任务
  │    │    ├─ 无任务 → 等待5秒 → 继续循环
  │    │    └─ 有任务 → 进入发送流程
  │    └─ ...
  └─ ...
```

---

## 三、详细节点配置

### 第1步：初始化变量

**节点**：【变量】→【设置变量】

拖入一个【设置变量】节点，配置以下变量：

| 变量名 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| `base_url` | 字符串 | `http://127.0.0.1:8000` | Python服务地址 |
| `worker_id` | 字符串 | `yingdao-01` | 当前机器标识（每台机器要不同） |
| `current_task` | 字典 | （空） | 当前任务数据 |
| `is_running` | 布尔值 | `True` | 循环控制标志 |

**配置截图示意**：
```
[设置变量]
├── base_url = "http://127.0.0.1:8000"
├── worker_id = "yingdao-01"
├── current_task = {}
└── is_running = True
```

---

### 第2步：创建循环容器

**节点**：【流程控制】→【While循环】

1. 拖入【While循环】节点
2. 条件设置为：`is_running == True`
3. 这个循环会让流程一直运行，不断拉取任务

---

### 第3步：拉取任务（HTTP请求）

**节点**：【网络】→【HTTP请求】

在While循环内部拖入HTTP请求节点：

**基础配置**：
- **请求方法**：`GET`
- **请求URL**：`{{base_url}}/outbox/next`
- **请求参数**：
  - 参数名：`worker_id`
  - 参数值：`{{worker_id}}`

**高级配置**：
- **超时时间**：`15` 秒
- **重试次数**：`2` 次

**输出设置**：
- 将响应内容保存到变量：`api_response`

**配置截图示意**：
```
[HTTP请求]
├── 方法: GET
├── URL: {{base_url}}/outbox/next
├── 参数:
│   └── worker_id: {{worker_id}}
├── 超时: 15秒
└── 输出: api_response
```

---

### 第4步：解析响应

**节点**：【变量】→【设置变量】

拖入设置变量节点，解析HTTP返回的数据：

**变量1**：`task_data`
- 值类型：Python表达式
- 表达式：`json.loads(api_response["body"])["task"]`

**变量2**：`has_task`
- 值类型：Python表达式
- 表达式：`task_data is not None`

---

### 第5步：判断是否获取到任务

**节点**：【流程控制】→【If条件】

拖入If条件节点：

**条件**：`{{has_task}} == True`

- **True分支**：有任务，执行发送流程
- **False分支**：无任务，等待后继续循环

---

### 第6步：无任务时的处理（False分支）

1. 拖入【执行Python代码】节点
2. 输入代码：
```python
import time
time.sleep(5)  # 等待5秒再拉取
```

3. 连接回While循环继续

---

### 第7步：有任务时的处理（True分支）

#### 7.1 提取任务信息

**节点**：【变量】→【设置变量】

设置以下变量（从task_data中提取）：

| 变量名 | Python表达式 |
|--------|--------------|
| `task_id` | `task_data["task_id"]` |
| `contacts` | `task_data["target_contacts"]` |
| `review_text` | `task_data["review_text"]` |
| `image_paths` | `task_data["image_paths"]` |

#### 7.2 激活微信窗口

**节点**：【界面自动化】→【激活窗口】

- **窗口标题**：`微信`（或留空使用上一次匹配的窗口）
- **匹配方式**：包含
- **超时**：`5` 秒

#### 7.3 循环联系人

**节点**：【流程控制】→【ForEach循环】

- **循环对象**：`{{contacts}}`
- **当前项变量名**：`contact_name`

在这个循环内处理每个联系人的发送逻辑。

#### 7.4 搜索联系人

**节点**：【界面自动化】→【键盘输入】

1. 按 `Ctrl + F` 打开微信搜索框

**节点**：【界面自动化】→【输入文本】

1. 点击搜索框（坐标或图像识别）
2. 输入文本：`{{contact_name}}`
3. 等待：`1` 秒

**节点**：【界面自动化】→【键盘输入】

1. 按 `Enter` 键进入聊天
2. 等待：`2` 秒

#### 7.5 校验联系人（重要！防误发）

**节点**：【界面自动化】→【获取文本】

- 获取聊天窗口标题栏的联系人名称
- 保存到变量：`actual_contact`

**节点**：【流程控制】→【If条件】

- 条件：`{{actual_contact}} == {{contact_name}}`
- 如果不匹配，截图并跳到失败处理

#### 7.6 发送文字评价

**节点**：【界面自动化】→【点击】

1. 点击输入框位置（或按 `Alt + S` 聚焦）

**节点**：【界面自动化】→【输入文本】

1. 输入文本：`{{review_text}}`

**节点**：【界面自动化】→【键盘输入】

1. 按 `Enter` 发送
2. 等待：`1` 秒

#### 7.7 发送图片（如有）

**节点**：【流程控制】→【ForEach循环】

- **循环对象**：`{{image_paths}}`
- **当前项变量名**：`img_path`

内部步骤：

1. **判断文件存在**：【If条件】→ 文件存在 `{{img_path}}`
2. **复制文件到剪贴板**：使用Python代码或Ctrl+C
3. **粘贴到微信**：`Ctrl + V`
4. **发送**：`Enter`
5. **等待图片发送完成**：`3` 秒

#### 7.8 标记当前联系人发送成功

可以用一个计数器或列表记录成功的联系人

---

### 第8步：发送结果回写

#### 8.1 成功回写

**节点**：【网络】→【HTTP请求】

- **方法**：`POST`
- **URL**：`{{base_url}}/outbox/ack-success`
- **请求体类型**：`JSON`
- **请求体内容**：
```json
{
  "task_id": "{{task_id}}",
  "worker_id": "{{worker_id}}",
  "note": "所有联系人发送成功"
}
```

#### 8.2 失败回写（失败分支使用）

- **方法**：`POST`
- **URL**：`{{base_url}}/outbox/ack-failure`
- **请求体**：
```json
{
  "task_id": "{{task_id}}",
  "worker_id": "{{worker_id}}",
  "step": "搜索联系人",
  "error_message": "未找到联系人",
  "screenshot_path": "{{screenshot_path}}"
}
```

---

### 第9步：异常处理与截图

在每个关键步骤（搜索、发送）后添加【Try-Catch】：

1. 正常逻辑放 Try 块
2. Catch 块中：
   - 截图保存到指定目录
   - 调用失败回写接口
   - 记录错误日志

**截图节点**：【界面自动化】→【截图】
- 保存路径：`D:/yingdao_screenshots/fail_{{timestamp}}.png`

---

## 四、完整流程图

```
【开始】
   ↓
【初始化变量】base_url, worker_id
   ↓
【While循环：is_running】
   │
   ├─→ 【HTTP请求：GET /outbox/next】
   │     ↓
   ├─→ 【解析响应】task_data
   │     ↓
   ├─→ 【If：has_task？】
   │        │
   │    False │
   │        ↓
   │    【等待5秒】
   │        │
   │    True │
   │        ↓
   │    【提取任务信息】
   │        ↓
   │    【激活微信窗口】
   │        ↓
   │    【ForEach：contacts】
   │          │
   │          ├─→ 【Ctrl+F 搜索】
   │          ├─→ 【输入联系人名】
   │          ├─→ 【Enter 进入聊天】
   │          ├─→ 【校验联系人名称】
   │          │         │
   │          │    不匹配│
   │          │         ↓
   │          │     【截图】→【失败回写】→【Break】
   │          │
   │          ├─→ 【粘贴文字】
   │          ├─→ 【Enter发送】
   │          ├─→ 【ForEach：image_paths】
   │          │       ├─→ 【复制图片】
   │          │       ├─→ 【粘贴发送】
   │          │       └─→ 【校验发送成功】
   │          │
   │          └─→ 【标记联系人完成】
   │
   ├─→ 【HTTP：ack-success】
   │     ↓
   └─→ 【继续循环】
```

---

## 五、测试验证

### 5.1 准备测试数据

在浏览器或PowerShell执行：

```powershell
python -c "import requests, json; payload={'target_contacts':['自己的微信名'],'review_text':'这是一条测试评价，请忽略','image_paths':[],'file_paths':[]}; r=requests.post('http://127.0.0.1:8000/outbox/enqueue',json=payload); print(r.json())"
```

### 5.2 运行影刀流程

1. 确保微信窗口可见
2. 点击影刀的【运行】按钮
3. 观察影刀执行过程

### 5.3 查看结果

```powershell
# 查看任务状态
python -c "import requests, json; r=requests.get('http://127.0.0.1:8000/outbox/tasks'); print(json.dumps(r.json(),ensure_ascii=False,indent=2))"
```

---

## 六、常见问题

### Q1: HTTP请求报错"连接 refused"
**解决**：检查Python服务是否启动，`app.py`是否在运行

### Q2: 微信搜索不到联系人
**解决**：
- 检查微信窗口是否激活
- 检查联系人名称是否完全匹配（包括空格）
- 建议先用自己的微信测试

### Q3: 发送时输入法不对
**解决**：
- 在影刀流程开始前，强制切换为英文输入法
- 或使用【执行Python代码】控制输入法

### Q4: 图片发送失败
**解决**：
- 检查图片路径是否存在
- 检查图片是否被其他程序占用
- 增加发送后的等待时间

---

## 七、优化建议

1. **设置定时调度**：影刀中可以设置每5分钟运行一次流程
2. **配置异常报警**：连续失败超过阈值时发送邮件/短信
3. **日志记录**：每个步骤记录日志到本地文件
4. **心跳机制**：定期上报心跳让Python侧知道影刀在线

---

## 八、参考文档

- `YINGDAO_SOP.md` - 运维SOP
- `YINGDAO_FLOW_BLUEPRINT.md` - 协议蓝图
- `yingdao_polling_client.py` - Python客户端参考代码
